spring:
  application:
    name: hlt-bmr-calc-service
  zipkin:
    base-url: http://localhost:9411

server:
  port: 8084

eureka:
  client:
    fetchRegistry: true
    registerWithEureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka

springdoc:
  swagger-ui:
    operationsSorter: method